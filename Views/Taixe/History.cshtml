@model List<WedNightFury.Models.Order>
@{
    ViewData["Title"] = "Lịch sử giao hàng";
}

<h2 class="fw-bold mb-3">🕒 LỊCH SỬ GIAO HÀNG (@ViewBag.Days ngày gần nhất)</h2>

@if (!Model.Any())
{
    <div class="alert alert-info">Chưa có dữ liệu lịch sử.</div>
}
else
{
    <table class="table table-striped table-bordered">
        <thead class="table-dark">
        <tr>
            <th>Ngày</th>
            <th>Mã đơn</th>
            <th>Khách</th>
            <th>Địa chỉ</th>
            <th>Trạng thái</th>
            <th></th>
        </tr>
        </thead>
        <tbody>
        @foreach (var o in Model)
        {
            <tr>
                <td>@o.DeliveryDate?.ToString("dd/MM/yyyy")</td>
                <td>@o.Code</td>
                <td>@o.ReceiverName</td>
                <td>@o.ReceiverAddress</td>
                <td>@o.Status</td>
                <td>
                    <a asp-action="StopDetail" asp-route-id="@o.Id" class="btn btn-sm btn-outline-dark">Xem</a>
                </td>
            </tr>
        }
        </tbody>
    </table>
}
