@model WedNightFury.Models.Order

@{
    ViewData["Title"] = "Bản đồ giao hàng";
    Layout = "~/Views/Shared/_LayoutDriver.cshtml";
}

<style>
    #map {
        width: 100%;
        height: 550px;
        border-radius: 12px;
    }

    .info-box {
        background: #fff;
        padding: 12px;
        border-radius: 10px;
        box-shadow: 0px 2px 8px rgba(0,0,0,0.15);
        margin-bottom: 10px;
    }
</style>

<div class="container mt-3">

    <div class="info-box">
        <h4 class="fw-bold mb-1">📍 Điều hướng đến điểm giao</h4>
        <p class="m-0"><b>Đơn:</b> @Model.Code</p>
        <p class="m-0"><b>Người nhận:</b> @Model.ReceiverName</p>
        <p class="m-0"><b>Địa chỉ:</b> @Model.ReceiverAddress</p>

        <a target="_blank"
           href="https://www.google.com/maps/dir/?api=1&destination=@Model.Lat,@Model.Lng"
           class="btn btn-primary mt-2">
            🚀 Mở Google Maps điều hướng
        </a>
    </div>

    <div id="map"></div>

</div>

<script>
    function initMap() {
        const pos = { lat: @Model.Lat, lng: @Model.Lng };

        const map = new google.maps.Map(document.getElementById("map"), {
            zoom: 17,
            center: pos
        });

        new google.maps.Marker({
            position: pos,
            map: map,
            title: "Điểm giao hàng"
        });
    }
</script>

<script src="https://maps.googleapis.com/maps/api/js?key=YOUR_API_KEY&callback=initMap"
        async defer></script>
